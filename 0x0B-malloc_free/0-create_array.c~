#include "main.h"
/**
 *create_array - function that creates an array of chars
 *@size: size of array
 *@c: char to initialize array with
 *Return: NULL if it fails pointer to array.
 */
char *create_array(unsigned int size, char c)
