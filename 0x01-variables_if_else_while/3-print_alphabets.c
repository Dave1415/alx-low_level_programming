#include <stdio.h>
/**
 * main - is n positive and negative
 *
 * Return: 0 if successful
 */
int main(void)
{
char low;
for (low = 'a'; low <='z'; low++)
putchar(low);
/* print uppercase letters */
for (low = 'A'; low = 'Z'; low++)
putchar(up);
{
putchar('\n');
}
return(0);
}
