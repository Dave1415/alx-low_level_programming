#include "main.h"
/**
 *mul - multiplies two integers
 *@a: integers to be multiplied to b
 *@b: integers to be multiplied to a
 *Return: 1 if number is a digit else 0
 */
int mul(int a, int b)
{
return (a * b);
}
