#include "main.h"
/**
 *_isupper _ checks for uppercase character
 *@letter: the character needs to be tracked
 *Return: 1 if it is uppercase else 0
 */
int _isupper(int letter)
{
  return (letter >= 'A' && letter <= 'Z');
}
