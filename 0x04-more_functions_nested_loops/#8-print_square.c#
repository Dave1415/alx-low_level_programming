#include "main.h"
/**
 *print_square - prints a square
 *@size: size of the square
 *
 *Return: a square
 */
void print_squaintsize)
  int a, b;  if (size > 0)
    {
for (a = 0; a < size; a++)
{
for (b = 0; b < size; b++)
{
_putchar(35);
}
-putchar('\n');
}
}
else
_putchar('\n');
}
}